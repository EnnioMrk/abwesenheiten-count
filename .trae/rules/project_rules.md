1. Use Bun
2. Use Bun testing
3. Only use external node tools if bun does not include them
4. Only use external libraries if bun does not include them
5. Use CSS only when absolutely needed. Use tailwind otherwise.
6. Put html files in their own folders. Use structure like this: /page/index.html; /page/js/index.js; /page/css/style.css
